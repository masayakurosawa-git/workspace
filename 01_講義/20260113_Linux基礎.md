# 流れ
## 1ヶ月目
- AWSでサーバ（EC2）を構築
- ミドルウェアの学習
- ロラブルシューティングのロールプレイ
- セキュリティ
- シェルスクリプト


# 講義
- Linuxがインフラの世界でシェアが広い理由
・再起動がほとんど必要ないため。

- Webサーバ
ユーザがアクセスするための入り口。
クライアントからのHTTPリクエストを受け取り、アプリケーションサーバーの処理が必要なリクエストは、アプリケーションサーバに転送する。
静的なコンテンツ（HTMLや画像）であれば、WEBサーバがレスポンスを返す。
「Apache HTTP Server」や「NGINX」などがある。

- APサーバ
APサーバと略されることもある。Tomcat(Java)、(Apache(PHP)、)Unicorn(Ruby)などがある。
（PHPの場合は、ApcheがWEBサーバがAPサーバも兼ねている）
単体で使用されることはなく、WEBサーバから転送されたリクエストを、RubyやPHPなどのプログラムにより処理をして、レスポンスをWEBサーバに返す。

- DBサーバ
データが保存されているところ。



- クラウドサービス(？)
物理サーバを大量に保管し、メンテナンスをしてくれているサービス。

・物理サーバ
「オンプレミス」と呼ばれる。
メリットは、自社都合で操れる。
デメリットは、初期コストと時間がかかる。維持コストが発生。

・クラウドサーバ
メリットは、構築に時間がかからない。初期コストが安い。維持コストがかからない。
デメリットは、メンテナンスをAWSに合わせる必要がある。




- Linux開発者
linus Torvalds
1991年、フィンランドの一学生が開発。


- オープンソース
自分好みに改造して使用できる。


- ディストリビューション(流派)
「Linux を使いやすい形にまとめた“完成品セット”」
・Linuxカーネル
    エンジンだけの車 🚗（そのままでは走れない）

・ディストリビューション
    エンジン＋車体＋ハンドル＋ナビ付きの完成車 🚙

※AmazonLinux2023(RedHat系)を研修では使用。




# ユーザ
- ルートユーザ
- 一般ユーザ



# リモートログイン
sshコマンドで接続する。
.pemファイルも必要。（認証ファイル）



# AWS
EC2を使用するときのリージョンは「オレゴン」を選択
インスタンス = サーバ
ファイアーウォール(FW) = セキュリティグループ(SG)


サーバ立てる
インスタンス起動
↓
タグ追加（StopTime-pm8）
↓
キーペア設定（ssh接続、ED25519、.pemで作成）
↓
セキュリティグループ設定
↓
インスタンスを起動


- インバウンドルール
外から内に入ってくるものに対するルール。
タイプ：許可するアクセスタイプ。
ソース：誰からのアクセスを許可するか。


- アウトバウンドルール
内から外に出ていくものに対するルール。


- EC2接続
chmod 600 ~/.ssh/CL_kurosawa.pem
ssh -i ~/.ssh/CL_kurosawa.pem ec2-user@34.222.149.122



- ファイル転送
scp -i ~/.ssh/CL_kurosawa.pem /Users/kurosawamasaya/Downloads/dummy.csv ec2-user@34.222.149.122:/home/ec2-user/


- ユーザ作成
・rootユーザへスイッチ
sudo su -
・ユーザ追加
useradd kurosawa




# 演習
- 問題1
awk -F, '{print $5}' dummy.csv | grep -c "女"

- 問題2
awk -F, '{print $6}' dummy.csv | sort | uniq -c

awk -F, '{print $6}' dummy.csv | grep -c '^A$'
awk -F, '{print $6}' dummy.csv | grep -c '^B$'
awk -F, '{print $6}' dummy.csv | grep -c '^AB$'
awk -F, '{print $6}' dummy.csv | grep -c '^O$'

- 問題3
awk -F, '{print $7}' dummy.csv | cut -f2 -d@ | sort | uniq -c


- 問題4
awk -F, '{print $5,$6}' dummy.csv | sed 's/ //g' | grep '^女A$' | wc -l


- 問題5
awk -F, '$3+0 >= 30 && $3+0 < 39 {print $3+0}' dummy.csv | wc -l

