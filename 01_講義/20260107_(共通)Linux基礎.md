# Linux入門
/home ユーザのホームディレクトリ
/etc システム全体に関わる設定ファイルが格納されている（エトセ）
/var 内容が常に可変するデータが格納される。（例：ログファイル）
/tmp 一時的なファイル置き場（テンプ）

## ショートカット
Ctrl + a 入力中の先頭にカーソルを移動
Ctrl + e 入力中の最後にカーソルを移動
Ctrl + u カーソルから前の文字をすべて削除
Ctrl + k カーソルから後ろの文字をすべて削除
Ctrl + w カーソルから前の文字を単語単位で削除
Ctrl + l ターミナルの画面をクリアする
Ctrl + r 過去のコマンドから指定の文字列で検索
↑ 　　　　以前入力したコマンドを呼び出す事ができます。↓で戻る事もできます
Tab キー 例えば、less を les まで入力してから tab を押すことでコマンドを保管してく　　　　　れます。複数の補完候補がある場合は tab を２回押すことで補完する候補を表示し　　　　　てくれます。
　　　　　さらにコマンドだけでなくパスの入力にも使えます。


# ユーザ
rootユーザ　特権管理者（使用には気をつけること）
一般ユーザ　


# AWS横山さんサーバに接続（US）
cd
chmod 600 ~/learnlinux.pem
ssh -i ~/learnlinux.pem ec2-user@34.215.70.223

# コマンド
## ディレクトリの移動
$ cd /var/tmp /var/tmp ディレクトリに移動します
$ cd ../ 1 個上の階層に移動します。左記例だと/var へ移動
$ cd - 直前にいたディレクトリに戻ります
$ cd 引数無しで移動するとユーザのホームディレクトリに移動します
$ pwd 現在のディレクトリ位置を表示してくれます

## ファイル・ディレクトリの一覧を表示
$ ls 引数無しで実行すると現在の場所の一覧を表示してくれます
$ ls -l -l オプションは詳細な情報を表示してくれます
$ ls -a 隠しファイル・ディレクトリも含めて表示してくれます
$ ls /home/yamada 引数に与えたディレクトリの一覧を表示してくれます
$ ls -la /home/yamada オプション・引数を組み合わせる事が可能です

## ファイル等の操作
$ touch file1　file1というファイルを作成します
$ mkdir dir1　dir1ディレクトリを作成します
$ rm file1　file1を削除します
$ rm -r dir1 dir1 ディレクトリを削除
　　　　　　※ディレクトリを削除するときは-r オプションをつける必要があります
$ rmdir dir1 空ディレクトリのみ削除（安全）
$ mv file1 ./dir1/file1　file1を./dir1ディレクトリに移動します。
$ mv file1 file2 　第2引数を同じディレクトした場合はリネームができます
$ cp file1 ./dir1/file2　file1をdir1ディレクトリ配下にfile2としてコピーします
$ cp -r dir1 dir2　dir1をdir2としてディレクトリコピーします
　　　　　　※ディレクトリをコピーするときは-r オプションをつける必要があります

## ファイルの閲覧
$ cat file1　標準出力で中身を確認できる。
$ less file1　ターミナル上のエディタで中身を確認できる。
$ tail file1　ファイルの末尾から表示し確認することができる。
$ tail -f file1 ファイル更新をリアルタイムで監視し続ける


## ファイルへの入力
vi

i 挿入モードに切り替え（カーソルの左から入力を開始）
a 挿入モードに切り替え（カーソルの右から入力を開始）
o 挿入モードに切り替え（カーソルの 1 行下から入力を開始）
:行番号 例えば:10 と入力すると 10 行目に移動
:q viを終了
:q! viを強制終了(保存せずに終了したいときに使う)
:wq wとqは同時に使うことができる。この場合は保存して終了

! エクスクラメーションマーク


# 演習
ホームディレクトリに移動
cd

ディレクトリ作成
mkdir kurosawa.masaya

作成したディレクトリに移動
cd /home/ec2-user/kurosawa.masaya

ファイル作成
touch /home/ec2-user/kurosawa.masaya/file1

## リダイレクト
追記
$ echo “今日の晩御飯は何にしよう” > hoge.txt
上書き
$ echo “焼肉にしよう” >> hoge.txt

# パイプ
ls | grep kurosawa



$ awk -F, '{ print $2 }' hoge.txt
-F,　区切り文字を指定
'{ print $2 }'　列指定
hoge.txt　ファイル名



sortとuniq
ソートしてユニークで重複削除



# パーミッション
drwxr-xr-x

d　ディレクトリ
-　ファイル
rwx　所有ユーザ
r-x　所有グループ
r-x　その他ユーザ



rm file_kurosawa2
touch file_kurosawa3
touch file_kurosawa4

# 複数行を一括でファイルに出力する
cat <<EOS > file_kurosawa
1:Suzuki:AB
2:Aoki:A
3:Fukada:O
4:Okumura:B
EOS



ssh -i ~/learnlinux.pem ec2-user@34.215.70.223

mkdir $HOME/dir_masaya.kurosawa