# Gitとは
バージョン管理システム

# git設定
git config

# Git基礎
### ローカルバージョン管理
このシステムはファイルのバージョンごとの変更をデータベースに保存します。

### 集中バージョン管理システム
すべてのファイルをバージョン管理するシステムをサーバ上に構築し、各開発者はそこからファイルをコピー（チェックアウトと呼びます）することで、いつ誰がどのように変更を行ったか辿れるようになりました。

### 集中バージョン管理システム
開発者は変更するファイルをチェックアウトするのではなく、リポジトリ（アプリケ
ーションのすべてのファイルを格納したディレクトリとその履歴）全体のコピーをローカルに
持ちます。すべてのコピーを複数の開発者が持っているため、サーバに障害が発生したとして
も、いずれかの開発者のリポジトリをコピーしてすぐにサーバを修復することができます。

# 三つの状態
- コミット済
ステージ済のファイルの変更を記録すると、コミット済になります。

- 修正済
ファイルを変更し保存した状態が修正済です。

- ステージ済
修正済のファイルに、これから履歴として保存するための印がついた状態がステージ
済です。


# GIT演習
echo "2回目の修正です。" >> first.txt;


echo "mkdir ~/git-practice" >> README.md;
echo "cd git-practice" >> README.md;
echo "git init" >> README.md;
echo "touch README.md" >> README.md;
echo "echo "# Git 演習" >> README.md" >> README.md;
echo "cat README.md" >> README.md;
echo "git status" >> README.md;
echo "git add README.md" >> README.md;
echo "git status" >> README.md;
echo "git commit -m "演習１のコミットです。"" >> README.md;
echo "git status" >> README.md;



# ブランチの作成
git branch testing

# ブランチの確認
git branch

# ブランチの切り替え
git checkout testing


vi README.md
git status
git add README.md
git status
git commit README.md


# GithubにSSHキーを登録
ssh-keygen -t ed25519 -C "masaya.kurosawa@rakus-partners.co.jp"


# リモートリポジトリのクローン
git clone git@github.com:masayakurosawa-git/test_20260108.git

# クローン後の初期設定
echo "# test_20260108" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:masayakurosawa-git/test_20260108.git
git push -u origin main


# CL向け主要コマンド
- リモートブランチの取得
最新のリモートリポジトリを取得する。自分の開発環境を最新状態にする。
git fetch origin

- ブランチ切り替え
git checkout

